# Runtime dependencies. Authoritative source is pyproject.toml [project.dependencies];
# this file exists for `pip install -r requirements.txt` convenience and must be kept
# in sync with pyproject.toml.
pyyaml>=6.0
psutil>=5.9.0
plotly>=5.0.0
tqdm>=4.64.0
pandas>=1.5.0
